import { Image, ImageProps } from '../../base/Image'
import blackRook from '../../../assets/pieces/rook-b.svg'

export const BlackRook = (props: ImageProps) => (
  <Image src={blackRook} {...props} />
)
